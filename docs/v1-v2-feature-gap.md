# v1 → v2 機能ギャップ一覧（アーカイブ・v1削除は完了済み）

> **本ドキュメントは調査・意思決定の記録として保存している。v1本体（`index.html`, `assets/v1/*`）は
> 2026-07-19付で削除済み。** 引用しているファイルパス（`assets/v1/app.js:75-89` 等）はワーキング
> ツリーには存在しないが、git履歴からは参照可能（v1削除前の最終コミット: `d88c683`）。
>
> 元の目的: v1（`index.html`）にのみ存在し、v2ウィザード（`gui-v2.html`）への移植方針が
> まだ確定していない機能だけを追跡する記録だった。全項目の方針確定・対応が完了し、v1本体削除に至った。
>
> 廃止・実装しないと決定済みの項目は本ファイルから削除済み。詳細根拠が必要な場合は
> git履歴（commit `1b44465`, `8e9e492` ほか）と `docs/v1-legacy-reference.md` を参照。
>
> 生成ロジック本体（`logic.js` vs `logic-v2.js`）は**機能的な抜け漏れ無し確認済み**（v2はvalidationルールを含め v1 の厳密なスーパーセット）。

---

## 解決済み（詳細は削除・要点のみ）

- **デバッグパネル＋デバッグモード**: 廃止。v2側の残存コード削除済み（commit `8e9e492`）。
- **機械定義JSONエクスポート/インポート**: 廃止。v1(`debug.js`)専用機能のためv2に元々存在せず、追加対応不要。
- **値カテゴリ色分け表示（計算値/入力値/機械定数）**: 廃止。v2側の残存コード削除済み（commit `8e9e492`）。
- **差分ビュー／結果エリア手動編集ロック／オンスクリーンテンキー／入力欄クリアボタン／ワーク種別クイックリファレンス説明文**: v2に実装しない方針確定。
- **gui-v2.jsに一度実装されたデバッグボタンの残骸**: 確認の結果v2側に痕跡なし、対応不要。
- **オペレーターヒント吹き出し**: 廃止・全て削除。設計パターンは `docs/v1-legacy-reference.md` §4 に素材として保存済み。v2側の死んだ文言データ（`i18n-v2.js`の`*FocusHint`系11キー・`cpUsesIdDepthHint`）も削除済み。
- **スタイル説明の補足ノート**（「通常バイト加工」「ヨセ中継」横の※展開ノート）: 廃止。死んだ文言（`i18n-v2.js`の`styleNormalNote`/`yoseRelayNote`）も削除済み。
- **スタイル選択カードのアイコン（card-icon）**: 移植済み。ノート機能とは別に、6種の加工スタイルを視覚的に区別するCSSアイコンをv2の`buildStyleScreen`に追加（`gui-v2.js`の`STYLE_ICON_HTML`、`gui-v2.css`）。ブラウザ実機で全アイコンの表示を確認済み。
- **全角→半角の自動変換**: 対応済み。ID・NUMERICカテゴリ（図番・工程No・寸法などの数値系欄）に変換を追加復活（`assets/validators-v2.js`の`toHankaku()`＋`stripDisallowedChars()`の`convert`フラグ）。FREE_TEXTカテゴリ（作成者名）は消去のみを維持。変換のみで済んだ場合（誤りではなく正常な変換）は赤枠フラッシュを出さないよう区別済み。ブラウザ実機で全角数字の変換・不正文字の除去・作成者名欄の非変換を確認済み。
- **リアルタイムバリデーションのタイミング**: 対応済み。`logic-v2.js`の`generateGCode()`内の業種固有ルールを`validateBasicSelections`/`validateDrawNumAndAuthor`/`validateCommonNumericFields`/`validateStyleSpecificRules`の4関数に分離し、`validateDomainRules()`として合成（最終ゲート、`generateGCode()`から呼び出し・変更なし）。ウィザードの「加工深さ」画面の「次へ」押下時（`gui-v2.js`の`next-depths`アクション）に図番・作成者チェックを除く3関数を早期呼び出しし、idDepth>7等のルール違反をその場でトースト表示してブロックするよう変更。ロジックの二重実装なし。ブラウザ実機で不正値のブロックと訂正後の通過を確認済み。

## v2で既に同等以上に実装済み（参考・再確認不要）

- ツールパスプレビュー、生成履歴（localStorage永続化のスーパーセット）、入力値JSONのエクスポート/インポート、外径最大値(maxOD)自動計算ドロワー（4モード全て・SVG図解付き）、作成者クイックボタン、アテ長さプリセット、M99P100/X50.U8選択、MH系工具選択、G12Bノーズ R、交差穴・ヨセ・ヨセ中継の各サブパネル
- `blocks.js`/`data.js`/`i18n.js` は v1/v2 でほぼ内容が同一（`data.js`はv2側がNCL015もみつけブロックを1件多く持つのみ）

---

## 次のアクション

このドキュメントで洗い出した項目は全て解決済み。**v1本体（`assets/v1/*`, `index.html`）は削除済み。**
本ドキュメントは記録として保存し、以後の更新は行わない。
